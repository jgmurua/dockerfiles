# mysql-workbench in docker

## mysql-workbench in docker via browser

# docker-compose

## Usage

```bash
docker-compose up -d
```

# kubernetes

## Usage

```bash
kubectl apply -f k8s-manifest.yaml
```
